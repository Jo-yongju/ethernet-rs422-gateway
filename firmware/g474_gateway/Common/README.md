# Protocol V1 common source

These files are exact build copies of the canonical sources in the repository
root `common/` directory. Do not make G474-specific edits in this directory.
Update `common/` first, copy the six files here, and verify that their contents
remain identical before building the firmware.
